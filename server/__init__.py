"""Thin web layer for Saqua. Contains ONLY transport/serialization glue over the
existing backend (research engine, writer, chat, storage, tools) — no business
logic lives here and nothing in the backend is modified."""
